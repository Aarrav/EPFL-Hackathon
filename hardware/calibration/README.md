# Calibration Files

- `follower.json` contains the SO101 follower motor calibration.
- `leader.json` contains the SO101 leader motor calibration.

Keep machine-specific calibration files under this folder and point LeRobot commands at the matching file when needed.
